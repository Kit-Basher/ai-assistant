{
  "name": "runtime_status",
  "description": "Observe-only runtime diagnostics for service health.",
  "version": "0.1.0",
  "permissions": ["db:read", "net:none"],
  "functions": [
    {
      "name": "runtime_status",
      "args_schema": {"type": "object", "properties": {}}
    }
  ]
}
