# Personal Agent Release Bundle Uninstall

Run `bash uninstall.sh` to remove the installed runtime, launcher, and service.

By default, user state is preserved.

To remove user state too:

- `bash uninstall.sh --remove-state`

