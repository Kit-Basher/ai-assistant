{
  "name": "disk_pressure_report",
  "description": "Read-only disk pressure report with top dirs, growth, and largest files.",
  "version": "0.1.0",
  "permissions": ["db:read", "sys:read", "net:none"],
  "functions": [
    {
      "name": "disk_pressure_report",
      "read_only": true,
      "args_schema": {
        "type": "object",
        "properties": {
          "user_id": {"type": "string"}
        }
      }
    }
  ]
}
