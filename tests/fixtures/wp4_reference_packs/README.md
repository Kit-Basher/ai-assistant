# WP4 reference packs

These are disabled test fixtures, not bundled abilities. The executable fixture
stores auditable WAT source; tests compile it to the exact temporary `module.wasm`
artifact before local import. Nothing here is imported into live user state.
