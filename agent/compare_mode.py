from __future__ import annotations


def compare_now_to_what_if(text: str) -> str:
    """
    Placeholder implementation.

    Older revisions had an interactive "what-if" compare mode. The orchestrator imports this
    symbol, but the full feature is out of scope here; return a deterministic message.
    """
    _ = text
    return "Compare mode is not available in this build."

