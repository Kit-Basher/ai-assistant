# v0.2.16 — Safe Pack Capability Runtime proof closure

This is the exact WP4 release candidate. It contains the v0.2.15 safe pack
runtime unchanged and completes the release benchmark contract by measuring
the verified WP3 mixed native/pack task path with sample count, median, and
p95. The benchmark fails closed unless both the native and dynamic pack steps
complete with registry verification.

All WP1–WP4 gates, the complete Python and JavaScript suites, the locked Web
UI build, isolated candidate lifecycle/worker verification, and state
preservation checks apply to the exact v0.2.16 commit.
