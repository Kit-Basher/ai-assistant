# Personal Agent v0.2.27

Superseded by v0.2.28. The v0.2.27 product behavior passed its live RAM and
recovery journeys, while the browser smoke still treated truthful “ready”
recovery wording and an expected adversarial HTTP 400 as fatal, and could race
the bounded UI busy-state transition. v0.2.28 hardens the installed-product
proof without weakening product checks.

WP6 pre-reinstall candidate superseding v0.2.26.

- Aligns typed `system.status` input extraction with semantic routing so RAM
  pressure questions invoke the bounded resource observation and baseline
  verifier, not the generic host summary.
- Retains the concrete-domain priority and indirect Model Scout corrections
  from v0.2.25-v0.2.26.
- Retains the WP6 layered gate, diagnostics export, normal-user onboarding,
  portable backup/isolated restore proof, maintenance audit, and Ubuntu
  recovery preflight.
- Physical Ubuntu 24.04 fresh-install recovery remains deliberately unproved;
  this release may only be reported as the WP6 pre-reinstall gate.
