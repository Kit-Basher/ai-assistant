# Personal Agent v0.2.29

WP6 pre-reinstall candidate superseding v0.2.28.

- Retains all v0.2.27 runtime behavior and the v0.2.28 browser proof
  hardening.
- Treats only bounded `/tasks` connection-abort/reset/refusal evidence during
  the smoke's deliberate API outage as expected recovery traffic; failures
  outside that window remain release-blocking.
- Physical Ubuntu 24.04 fresh-install recovery remains deliberately unproved;
  this release may only be reported as the WP6 pre-reinstall gate.
